import * as React from 'react';
import { Icon } from '@ui-kitten/components';

const ForwardIcon = (props: any): React.ReactElement => (
  <Icon {...props} name="arrow-forward" />
);

export default ForwardIcon;

import * as React from 'react';
import { Icon } from '@ui-kitten/components';

const DeleteIcon = (props: any): React.ReactElement => (
  <Icon {...props} name="trash" />
);

export default DeleteIcon;
